enum Environment { local, staging, dev, prod }

enum CmsType { privacyPolicy, termsOfUse }

enum SuggestionScreenType { findFood, foodFound }

enum ScreenTypeEnum { findFood, foodFound }

enum MealCuisineType { meal, cuisine, items, subItems }

enum GoatType { add, replace, remove }

enum GoatBreakdownType { local, national }

enum SignupType { normal, guide }

enum UserType { normal, guide }

enum ProfilePalateType { local, national }

enum FavoriteSort { distance, createdAt, name }

enum AddedGoatType { local, national, both, none }
